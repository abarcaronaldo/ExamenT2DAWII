server:
  port: 8082

spring:
  application:
    name: ms-citas

  datasource:
    url: jdbc:mysql://localhost:3306/bdclinica?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

app:
  jwt:
    secret: "Cibertec_ProyectoIntegrador_Microservicios_ExamenT2_2026_KeySecure!"
    expiration-minutes: 60